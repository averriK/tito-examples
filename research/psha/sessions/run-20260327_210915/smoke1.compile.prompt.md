# Structured Prompt: Distance PDF for a Circular Areal Source

## SLOTS

### SLOT 1: Derivation of the distance probability density function for a central site within a circular areal source

The probability density function (PDF) of source-to-site distance $r$ is derived for a site located at the center of a circular areal seismic source zone of radius $R$, under the assumption of spatially uniform seismicity. The derivation proceeds from first principles: the problem geometry and the uniform spatial density over the circular area are defined, the cumulative distribution function (CDF) of distance $r$ is constructed by integrating the spatial density over concentric annuli, and the closed-form PDF $f_R(r)$ is obtained by differentiation. All intermediate algebraic steps appear explicitly, including the setup of the area element, the integration yielding the CDF, and the final expression for the PDF together with its domain of validity ($0 \leq r \leq R$).


## CONSTRAINTS

- All mathematical expressions use LaTeX notation ($...$ for inline, $$...$$ for display equations).
- The complete step-by-step derivation is presented, including all intermediate steps, not merely the final result.

