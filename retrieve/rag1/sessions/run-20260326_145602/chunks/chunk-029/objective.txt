RUNTIME CONTEXT
- Workflow: retrieve.clean
- Session: 1/1
- Model: sonnet
- Effort: high
- Executor contract: Claude Code CLI receives this objective via stdin and must emit only the manifest-defined artifact(s).
- Memory tools available under namespace 'tito.kramer2024s'; use only as cache, never cite memory as evidence.
RETRIEVE-CLEAN WORKFLOW (CHUNKED FORMAT-ONLY CLEANUP VIA MANIFEST)

You will perform the **format-only cleanup and citation normalization stage** of
`retrieve 2.x` on text that has already been extraído y cortado en slices por el
CLI.

Este workflow **no es un orquestador** y **no** diseña nuevas herramientas ni
pipelines. En su lugar:
- Lee un único manifiesto JSON `manifest.json` bajo `SESSION_DIR`.
- Usa exclusivamente ese manifiesto para descubrir qué slices existen y qué
  archivos `.md` debe escribir.
- Devuelve, en una única pasada, archivos `.md` limpios por chunk y un único
  `biblatex.bib`.
- Toda la lógica intermedia (tablas de referencias, checks, etc.) vive en
  memoria dentro de esta llamada; no se serializa a otros archivos.

Inputs:
- TASK_FILE: /home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/task.empty.md         # Opcional; directivas AI (NUNCA selección determinística)
- KB_DIR: /home/averri/github/tools/tito-examples/retrieve/rag1/kb/               # Opcional; solo lectura si hace falta contexto
- SESSION_DIR: /home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/chunks/chunk-029     # Directorio con archivos intermedios
- FORMAT_RULES: /usr/local/libexec/tito/rules/format.retrieve.md
- CITATIONS_RULES: /usr/local/libexec/tito/rules/citations.retrieve.md
- BIBLATEX_RULES: /usr/local/libexec/tito/rules/biblatex.md (opcional; reglas para biblatex)

Precondiciones (lo que ya hizo el CLI antes de llamar a este workflow):
- Se seleccionó exactamente un documento fuente (PDF o DOCX) y se extrajo a un
  único archivo de texto lineal bajo `SESSION_DIR` (este workflow no necesita
  leer ese archivo completo).
- El CLI ya creó **slices lineales** bajo `SESSION_DIR` y escribió un manifiesto
  JSON que enumera **exactamente** los slices y los `.md` de salida:
  - `chunk-001.slice.txt`, ..., `chunk-NNN.slice.txt`
  - `/home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/chunks/chunk-029/manifest.json`
- Opcionalmente, puede existir `biblatex.bib` en `SESSION_DIR`; si existe,
  debes reutilizarlo y refinarlo, no ignorarlo.

El manifiesto tiene esta forma (esquema conceptual, no lo reescribas):

```json
{
  "id": "",
  "source": {
    "basename": "<source.basename>"
  },
  "helper": {
    "assets_json": ".retrieve.pdf.assets.json",
    "tables": {
      "detected_count": 0,
      "markdown_path": ".retrieve.tables.md",
      "audit_path": ".retrieve.tables.audit.json"
    }
  },
  "chunks": [
    {
      "id": "chunk-001",
      "slice_path": "chunk-001.slice.txt",
      "clean_md_path": "chunk-001.md"
    },
    {
      "id": "chunk-002",
      "slice_path": "chunk-002.slice.txt",
      "clean_md_path": "chunk-002.md"
    }
  ],
  "outputs": {
    "primary": ".retrieve.md",
    "biblatex": "biblatex.bib"
  }
}
```

Objetivo:
- Para cada entrada en `chunks[]` del manifiesto, producir **exactamente un**
  archivo Markdown limpio `clean_md_path` que:
  - Aplique `FORMAT_RULES` estrictamente como transformación **solo de formato**.
  - Use `CITATIONS_RULES` para normalizar citas de texto a `[@Key]`.
- Bajo `CITATIONS_RULES` + `BIBLATEX_RULES`, generar o refinar un único
  `biblatex.bib` tal que todas las citas `[@Key]` en los chunks tengan entradas
  correspondientes.

En todo momento, considera que:
- La única representación **persistente** de la bibliografía en disco es
  `biblatex.bib`.
- Cualquier estructura auxiliar (tablas de referencias, listas de citas,
  estadísticas) vive dentro de esta sesión y **no** se serializa a archivos
  adicionales en `SESSION_DIR`.

Method:

0a) Read TASK_FILE (optional directives)
- If `TASK_FILE` is readable and non-empty: read it into the conversation.
- Treat task content as **AI directives only**.
- Ignore any `Document:` / `Documents:` keys if present (deprecated); they MUST NOT affect input selection.
- Never change which chunks you process: chunk discovery MUST come only from `manifest.json`.
- Any directives must still respect `FORMAT_RULES` (format-only default; no summarization/translation).

0) Confirm preconditions and load manifest

- Work ONLY under `SESSION_DIR`. Do not modify any files under KB_DIR.
- Read `/home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/chunks/chunk-029/manifest.json` into the conversation.
- Parse conceptualmente sus campos (`source`, `chunks`, `outputs`); no hace
  falta reescribir el JSON.
- Deriva de ahí:
  - `source.basename` (string con el basename del texto extraído).
  - `helper.tables.detected_count` (si está presente) como señal autoritativa
    del helper determinístico sobre cuántas tablas detectó realmente.
  - La lista ordenada de chunks (`chunks[]`).
- No uses `Glob`/`find` para descubrir slices o `.md`; **siempre** usa los
  nombres exactos listados en el manifiesto.

1) Initialize the global References table

- If `SESSION_DIR/biblatex.bib` already exists AND appears non-empty:
  - Treat it as the canonical reference table for this run.
  - Parse it only to the extent needed to map keys to reference metadata; do
    not rewrite or reformat entries unless explicitly required by
    `BIBLATEX_RULES`.
- Otherwise, comienza con una tabla de referencias vacía y ve poblando/
  refinando esa tabla **mientras procesas los slices** en el paso 2, usando
  únicamente el texto que aparezca en secciones `References` / `Bibliography`
  dentro de los propios slices.
- The outcome of this step is a **global** mapping from in-text citation
  patterns to stable keys `@Key` that you will use consistently across all
  chunks.

2) Per-chunk cleanup (format-only) and citation normalization

For each chunk entry in `manifest.chunks` (in order):

2.1 Read only this slice (paged Read)
- Construct the absolute path to the slice as
  `/home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/chunks/chunk-029/<chunk.slice_path>` usando exactamente el string de
  `slice_path`.
- Read the slice using paged `Read` calls with `offset` and `limit` (never read
  the full slice in one call).
  - Start with `offset=1, limit=200`.
  - Then read with `offset=201, limit=200`.
  - If needed, continue with `offset=401, limit=200`, etc. until the slice is
    fully read.
- You MUST NOT read the entire full-document extracted `.txt` file into Claude at once.
- You MUST NOT create alternative full-document cleaned copies (like
  `body_cleaned.txt`) or any other global mirror of the whole document.

2.2 Apply format-only cleanup within this chunk
- Apply `FORMAT_RULES` exactly as defined in `rules/format.retrieve.md`:
  - Remove obvious PDF artifacts (headers/footers/page numbers).
  - Normalize whitespace, paragraphs, lists, and headings when clearly
    indicated by the text.
  - Preserve all substantive sentences and their order.
  - Do NOT summarize, condense, reorder, or rewrite arguments.
- Keep the language of the source; do not translate.
- Treat fenced/indented code blocks as opaque; do not rewrite or normalize anything inside preformatted/code regions.
- Treat math normalization to LaTeX as the only allowed exception to
  "no rewriting", following the math section of `FORMAT_RULES`.

2.2a Respect helper table authority
- The canonical table-detection signal comes from `manifest.helper.tables`.
- If `manifest.helper.tables.detected_count` is present and equals `0`:
  - Treat that as authoritative that the deterministic helper did **not**
    detect canonical tables for this document.
  - You MUST NOT reconstruct Markdown tables from linearized or tabular-looking
    text in the slice.
  - Keep such material as plain text, list-like lines, or short paragraphs
    unless the slice already contains an explicit Markdown table verbatim.
- If `detected_count` is greater than `0`, you may preserve clearly visible
  table structure already present in the slice, but you still MUST NOT invent
  additional canonical tables beyond what the slice unambiguously shows.

2.3 Normalize in-text citations to [@Key]
- Within this chunk, detect and normalize citations according to
  `CITATIONS_RULES`:
  - Use the global references table from step 1.
  - Replace recognized citations with `[@Key]` when there is a clear
    correspondence.
  - Do not invent new keys; if a citation cannot be confidently mapped to a
    known reference, leave it in a neutral textual form and do not fabricate
    a BibLaTeX entry for it.
  - Do not normalize or introduce `[@Key]` markers inside fenced/indented code blocks.

2.4 Exclude the References section from body chunks
- If this slice contains part of the `References` / `Bibliography` section:
  - Use that text only to improve the global references table and
    `biblatex.bib`.
  - Do NOT include the list of references itself in the cleaned Markdown
    body for this chunk.

2.5 Write the per-chunk Markdown file (MANDATORY)
- For each chunk entry, you MUST write exactly one corresponding cleaned
  Markdown file under SESSION_DIR usando el `clean_md_path` del manifiesto.
- Ensure each `clean_md_path` file is small enough to be produced in a single
  response.

3) Generate or refine biblatex.bib

- Using the global references table and `BIBLATEX_RULES`, ensure that
  `SESSION_DIR/biblatex.bib` exists and satisfies these constraints:
  - There is exactly one entry per citekey `@Key` used in any chunk.
  - Each entry contains the minimal required fields (author, title, year,
    and venue/doi/url as appropriate).
  - Keys and fields follow the naming and normalization rules in
    `rules/biblatex.md`.
- If a preliminary `biblatex.bib` already existed, update or augment it
  carefully; do NOT discard or radically reformat well-structured entries
  unless they violate `BIBLATEX_RULES`.

4) Final confirmation

In your final response **do not print the document contents**. Instead,
explicitly confirm (in plain text) that you have:
- Written one cleaned Markdown file per chunk listed en `manifest.chunks`:
  - `clean_md_path` para cada entrada.
- Generated or updated:
  - `/home/averri/github/tools/tito-examples/retrieve/rag1/sessions/run-20260326_145602/chunks/chunk-029/biblatex.bib`.
- Used KB_DIR only as read-only context (if at all), with all new files
  written under SESSION_DIR.

Constraints:
- Do NOT re-extract from PDF/DOCX; assume all extraction and slicing has been
  done by the CLI or a prior workflow.
- Do NOT call any MCP helpers in this workflow; treat all inputs as plain text
  and file paths discovered via `manifest.json`.
- Do NOT create additional full-document cleaned files besides the required
  per-chunk `.md` files enumerados en el manifiesto.
- All intermediate and final outputs for this workflow live under SESSION_DIR;
  KB_DIR is strictly read-only.