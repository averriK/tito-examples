# Mathematical Equations

The extracted content consists primarily of mathematical equations related to seismic analysis. Due to extraction artifacts from the source PDF, many equations are corrupted or incomplete. Below is the cleaned representation preserving the visible mathematical relationships:

## Seismic Coefficients

$$k_h \approx = \cdot()$$

$$= (  ) \qquad = \frac{(  )}{(  )}$$

$$= + \frac{(  )}{(  )}$$

$$+ (  ) = = \frac{}{(  )}$$

$$= \kappa$$

where $\kappa$ is defined with parameters: $1 = (1)$, $T = 1s$

$$= (\cdot) \qquad T = T_s \qquad =$$

## Spectral Acceleration and PGV Relations

$$(  )$$

$$= () + [] + [] (  ) =$$

$$(  )$$

$$k_y = k_y(D_a, k_h, \text{PGV})$$

$$\frac{\partial k_y}{\partial k_h} = \frac{\partial k_y}{\partial D_a}(k_h) + (\text{PGV}) =$$

$$= (  ) \qquad k_h = g(\text{SSE})$$

$$k_h = k_h = g(\text{SSE})$$

$$=$$

$$T_s =$$

## Spectral Acceleration Values

$$S_a \approx g \qquad S_a \approx g$$

## Moment Magnitude

$$M_w = \qquad M_w =$$

$$= () + [] + [] (  )$$

## Operating and Safety Evaluation Earthquakes

$$= k_h \cdot g \text{ (OBE)}$$

$$k_h =$$

$$= k_h \cdot g \text{ (SSE)}$$

$$k_h = + \frac{S_h}{} \qquad = k_h \cdot g \text{ (OBE)}$$

$$k_h \qquad = k_h \cdot g \text{ (SSE)}$$

$$S = S_a(T = s) = g \qquad S = S_a(T = s) = g$$

---

**Note**: This document was extracted from a source with significant formatting issues. Many equation components are missing or corrupted, indicated by empty parentheses and incomplete expressions. The mathematical structure and variable relationships have been preserved as accurately as possible given the extraction quality.
